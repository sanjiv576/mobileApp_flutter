import 'package:flutter/material.dart';
// This dart file contains only constant properties

// used in input_page dart file --> Note: constant variable name starts with 'k', just a standard convention
const kBottomContainerHeight = 80.0;
const Color activeCardColour = Color(0xFF1D1E33);
const Color kBottomContainerColour = Color(0xFFFEB1555);
const Color inactiveCardColour = Color(0xFF111328);

const kValueLableTextStyle =
    TextStyle(fontSize: 50.0, fontWeight: FontWeight.w900);

const kLargeButtonTextStyle =
    TextStyle(fontSize: 25.0, fontWeight: FontWeight.bold);

// used in icon_content file
const kLabelTextStyle = TextStyle(color: Color(0xFF8D8E98), fontSize: 18.0);

// used in results_page file

const kTitleTextStyle = TextStyle(fontSize: 50.0, fontWeight: FontWeight.bold);

const KHealthStatusTextStyle = TextStyle(fontSize: 22.0, color: Color(0xFF34D876), fontWeight: FontWeight.bold);

const kCalculatedValueTextStyle = TextStyle(fontSize: 100.0, fontWeight: FontWeight.bold);

const kMessageTextStyle = TextStyle(fontSize: 22.0, color: Colors.white);

 
